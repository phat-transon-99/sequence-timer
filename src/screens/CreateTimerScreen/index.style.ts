import { StyleSheet } from 'react-native';
import { MARGIN_LARGE, PADDING_SMALL } from '../../styles/dims';

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: PADDING_SMALL,
  },
  spaced: {
    marginBottom: MARGIN_LARGE,
  },
});
