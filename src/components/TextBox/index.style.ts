import { StyleSheet } from 'react-native';
import { GRAY_COLOR } from '../../styles/colors';
import { FONTFAMILY_REGULAR, FONTSIZE_LARGE, FONTWEIGHT_REGULAR } from '../../styles/fonts';

export default StyleSheet.create({
  textBox: {
    color: GRAY_COLOR,
    fontFamily: FONTFAMILY_REGULAR,
    fontSize: FONTSIZE_LARGE,
    fontWeight: FONTWEIGHT_REGULAR,
  },
});
